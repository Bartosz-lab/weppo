<div class="prodcontener"><!-- PRODCONTENER NIE MOŻE MIEĆ ZDARZENIA ONCLICK BO FORMULARZE NIE BĘDĄ DZIAŁAĆ -->
    <div style="float:left" onclick="location.href='/p/<%= id %>';"> 
            <img src=" <%= imgurl %> ">
    </div>
    <div style="float:left; width: 50%; padding-top: 1em;" onclick="location.href='/p/<%= id %>';">
        <div class="item-list-title"> 
            <%= name %>  
        </div>
        <br>
        <% if( mode == 0 ) { %>
        <div class="item-list-desc"> 
            <% for(let param of params) { %>
                <div> <%= param.key %>: <%= param.value %> </div>
            <% } %>
        </div>
        <br>  
        <% } %>     
        <div class="item-list-title"> 
            <%= price %>zł
        </div> 
    </div>

    <div style="float:left; width: auto; padding-top: 3em;">
        <% if( mode == 0 ) { %>
            <form>
                <input type="submit" value= "Dodaj do Koszyka">
            </form>
        <% } else if ( mode == 1) { %>
            
            Ilość: <input type="number" value="<%= quantity %>" onkeyup="AJAXset(/*ID, ilość*/)" onmouseup="AJAXset(/*ID, ilość*/)">
            <!--aktualizacja wartości za pomocą AJAX, jeśli 0 to usuwa-->
               
            <input type="button" value="Usuń z koszyka" onclick="AJAXSET(/*ID, 0*/)">
            <!--aktualizacja wartości za pomocą AJAX, wywołanie z 0-->
            
        <% } else if ( mode == 2) { %>
            Ilość: <br><%= count %><br>
            Cena całkowita: <br><%- price * count %> PLN
        <% } %>
        </div>
    <div style="clear:both"></div>
</div>