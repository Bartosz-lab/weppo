 <% if(user_role === role.Admin) { %>

    Zamówienia ADMINA
    HELP

<% } else if(user_role === role.Seller) { %> 
    
    Zamówienia SELERA
    HELP

<% } else if(user_role === role.Customer) { %> 
    
    Zamówienia KLIENTA
    HELP

<% } %> 