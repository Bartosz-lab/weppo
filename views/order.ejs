<!DOCTYPE html>
<html lang="en">
<%- include('head') %>
<body id="body">
<%- include('topbar') %>

<div class ="contener">

ID zamówienia: <%- id %><br>

  <% for( var i = 0; i < display.length; i++ ) { %> 
    <%- include('wide-item', {href: display[i].href, name: display[i].name, price: display[i].price, imgurl: display[i].imgurl, desc: display[i].desc, basket: false}) %>
    <% } %>
  <div style="clear:both"></div>
</div>

</body>
</html>