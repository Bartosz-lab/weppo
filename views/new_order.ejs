<!DOCTYPE html>
<html lang="en">
<%- include('./template/head') %>
<body id="body">
<%- include('./template/topbar') %>

<div class ="contener">

  <% if ( products.length == 0 ) { %>
    <div class = "main-page-title">Koszyk jest obecnie pusty.</div><br><br>
    Zapraszamy na zakupy.  
  <% } %>

  <% for(let product of products) { 
    product.mode = 1; %>
    <%- include('./components/wide-item', product) %>
  <% } %>

  
  Tu powinien być formularz z danymi do uzupełnienia tzn imie nazwisko telefon adres itp

  Wysyłane na ten sam adres POST

  <%= JSON.stringify(user) %>
  <%= JSON.stringify(adress) %>

  <% if ( products.length != 0 ) { %>
    <br><br>
    <form method="post" action="/order/new">
        <input type="submit" value="Złóż zamówienie">
    </form>
    <br>
  <% } %>


</div>
<div id="alert" class="alertBox"></div>
</body>
<script src="/scripts/basket.js"></script>
</html>